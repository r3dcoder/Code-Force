#include <bits/stdc++.h>

using namespace std;

int main(){


   // while(1){
        int n;
        cin>>n;
        for(int i=1; i<=n; i++){

        if(i%2==0)  cout<<"I love"<<" ";

        if(i%2==1){
            cout<<"I hate ";
        }

        if(n==i) cout<<"it "<<endl;
        else cout<<"that ";

        }


   // }
    }
